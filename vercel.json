{
  "version": 2,
  "buildCommand": "cd apps/web && pnpm db:generate && cd ../.. && turbo build --filter=web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "functions": {
    "apps/web/app/api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}
